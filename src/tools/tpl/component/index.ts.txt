/**
* ${ComponentName}
* @author ${username}
* @date ${curDate}
* @since ${version}
*/

import ${componentName} from './${componentName}'
export * from './${componentName}'
export default ${componentName}